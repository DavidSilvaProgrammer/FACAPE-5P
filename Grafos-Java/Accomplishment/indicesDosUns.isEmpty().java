List<Aresta> indicesDosUns = achaIndicesDosUns(matriz);

if (indicesDosUns.isEmpty()) {
    // A lista está vazia
    System.out.println("A lista está vazia.");
} else {
    // A lista não está vazia
    System.out.println("A lista não está vazia.");
}


/*
Você pode verificar se a lista está vazia utilizando o método isEmpty(). 
Este método retorna true se a lista estiver vazia, ou false caso contrário. 
Aqui está como você pode fazer isso com um if:

Neste exemplo, se indicesDosUns estiver vazio, será impresso "A lista está vazia.". 
Caso contrário, será impresso "A lista não está vazia.".
*/
